# Terraxen Plan File Parser

The aim of this application is to allow users to better inspect large plan files

## Run CLI

```
go run cmd/parser/ -planjson <plan json format file>
```

# Run Web

```
cd cmd/web
go run . -planjson <plan json format file>
```